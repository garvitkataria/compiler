func main() -> void {

    let n: integer;
    let flag: integer;
    flag = 0;

    let i: integer;
    i = 2;
    while (i <= num / 2) {
      // condition for nonprime number
      if (num / i == 0) then {
        flag = 1;
      }else ;
      i = i + 1;
    };

    write(flag);

 }
 $